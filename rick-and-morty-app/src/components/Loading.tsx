import { Error } from '../interfaces/types'

// const Loading: React.FC<Error> = ( loading = '', error = "" ) => {

//     if (loading) {
//         return <div>Loading...</div>;
//     }

//     if (error) {
//         return <div>Error: {error.message}</div>;
//     }

// };

// export default Loading;